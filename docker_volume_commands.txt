docker build --platform=linux/amd64 -t ocr_backend .

docker volume create --driver local --opt type=none --opt device=C:\Users\DELL\Desktop\ocr_data\templates --opt o=bind template_bind
docker volume create --driver local --opt type=none --opt device=C:\Users\DELL\Desktop\ocr_data\csv_reports --opt o=bind csv_bind

docker run --name ocr_app --volume template_bind:/app/templates --volume csv_bind:/app/reports ocr_backend

docker run --name ocr_app -e HOST_ROOT_DIR=$(pwd) -v /var/run/docker.sock:/var/run/docker.sock ocr_backend      # For mac, linux, and Unix
docker run --name ocr_app --net=host -e HOST_ROOT_DIR=%cd% -v /var/run/docker.sock:/var/run/docker.sock -v template_bind:/app/templates -v csv_bind:/app/reports ocr_backend        # For command prompt
docker run --name ocr_app -e HOST_ROOT_DIR=${PWD} -v /var/run/docker.sock:/var/run/docker.sock ocr_backend      # For powershell

docker run --name ocr_app -e HOST_ROOT_DIR=%cd% -v %cd%\ocr_data\templates:/app/templates -v %cd%\ocr_data\csv_reports:/app/reports ocr_backend        # For mac, linux, and Unix
docker run --name ocr_app -e HOST_ROOT_DIR=$(pwd) -v $(pwd)\ocr_data\templates:/app/templates -v $(pwd)\ocr_data\csv_reports:/app/reports ocr_backend  # For command prompt
docker run --name ocr_app -e HOST_ROOT_DIR=${PWD} -v ${PWD}\ocr_data\templates:/app/templates -v ${PWD}\ocr_data\csv_reports:/app/reports ocr_backend  # For powershell

